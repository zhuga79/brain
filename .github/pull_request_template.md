<!--
  ВАЖНО, прочитайте до отправки.

  Этот репозиторий — генерируемый срез приватного рабочего дерева. Он
  пересобирается скриптом, который стирает каталог целиком. Ваш коммит
  здесь не сохранится: он будет утрачен при следующей пересборке.

  Это не значит, что PR бесполезен. Он читается как предложение с готовым
  кодом, обсуждается и переносится в приватное дерево вручную. Авторство
  сохраняется в истории обсуждения и в сообщении коммита, но не в git blame.

  Если такой порядок для вас неприемлем — скажите об этом в PR или заведите
  issue до начала работы. Мы предупреждаем заранее, чтобы вы не потратили
  время впустую.
-->

## Что меняется

<!-- Одним абзацем: какая проблема решается. -->

## Как проверить

<!-- Команды. Если добавлен тест — какой и что он ловит. -->

```bash
bash tests/run.sh <шаблон>
```

## Чеклист

- [ ] Изменение поведения CLI сопровождается тестом в `tests/cases/`
- [ ] Тесты запускались через `tests/run.sh`, а не файлами напрямую
- [ ] Изменение контракта отражено в `tasks/SCHEMA.md`, `MEMORY.md` или `spec/`
- [ ] Архитектурное решение оформлено ADR `wiki/decision-*.md` и внесено в `wiki/decisions-log.md`
- [ ] Не нарушены решения из существующих ADR — либо нарушение обосновано
- [ ] Нет секретов, токенов и персональных данных

## Затронутые инварианты

<!--
  Если правка касается очереди задач, блокировок, парсера или публикации —
  напишите, какой инвариант мог пострадать. Именно там ошибка тихо теряет
  данные, а не падает.
-->

---

Отправляя PR, подтверждаю: правка распространяется на условиях Apache-2.0,
и я вправе её передать.
