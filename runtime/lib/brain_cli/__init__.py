"""Точки входа CLI, живущие внутри пакета.

Раньше каждая утилита была отдельным файлом в runtime/bin, который сам
подмешивал каталоги в sys.path и ставился копированием. Здесь код лежит в
пакете, поэтому его ставит pip, а файл в runtime/bin остаётся тонкой обёрткой
ради привычного пути запуска.
"""
