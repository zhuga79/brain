"""brain_federation package — Brain vault federation checks and plan execution."""
from __future__ import annotations

from .core import (
    SCHEMA_VERSION,
    SEVERITIES,
    TASK_ID_RE,
    AGENT_ID_RE,
    NODE_ID_RE,
    NODE_ID_ENV,
    NODE_ID_DEFAULT,
    NODE_GIT_EMAIL_ENV,
    NODE_GIT_EMAIL_CONFIG,
    FEDERATION_CONFIG_FILE,
    RUNTIME_PATHS,
    GENERATED_IGNORE_PATTERNS,
    SECRET_RE,
    Finding,
    brain_path,
    node_id,
    node_audit_extra,
    result,
    exit_code,
    has_block,
    emit,
    parse_task_file,
)
from .checks import (
    comparable,
    check_duplicates,
    check_cross_file_conflicts,
    path_matches,
    check_runtime_paths,
    check_raw_rewrites,
    check_wiki_curation,
    check_provider_matrix,
    check_possible_secrets,
    check_optional_secret_scanner,
    check_gitignore,
    check_learning_status_conflict,
    check_council_synthesis_stale,
)
from .git_ops import (
    git_run,
    is_git_repo,
    git_paths,
    git_name_status,
    git_show,
    git_head,
    frontmatter,
)
from .preflight import (
    parse_task_file_optional,
    collect_preflight,
    cmd_preflight,
)
from .plan import (
    utc_now,
    sha256_text,
    sha256_file,
    canonical_json,
    plan_id,
    task_refs,
    plan_task_imports,
    build_plan,
    load_plan,
    task_import_block,
    task_import_validation,
    live_task_conflicts,
    stale_plan_findings,
    acquire_import_lock,
    release_import_lock,
    append_imports_atomic,
    log_imports,
    import_result,
    source_state,
    plan_wiki_changes,
    required_confirmations,
    safe_wiki_rel,
    proposal_gate_findings,
    proposal_result,
    proposal_meta,
    write_proposal_artifacts,
    cmd_plan,
    cmd_import_tasks,
    cmd_write_wiki_proposals,
)

__all__ = [
    # core
    "SCHEMA_VERSION",
    "SEVERITIES",
    "TASK_ID_RE",
    "AGENT_ID_RE",
    "NODE_ID_RE",
    "NODE_ID_ENV",
    "NODE_ID_DEFAULT",
    "NODE_GIT_EMAIL_ENV",
    "NODE_GIT_EMAIL_CONFIG",
    "FEDERATION_CONFIG_FILE",
    "RUNTIME_PATHS",
    "GENERATED_IGNORE_PATTERNS",
    "SECRET_RE",
    "Finding",
    "brain_path",
    "node_id",
    "node_audit_extra",
    "result",
    "exit_code",
    "has_block",
    "emit",
    "parse_task_file",
    # git_ops
    "git_run",
    "is_git_repo",
    "git_paths",
    "git_name_status",
    "git_show",
    "git_head",
    "frontmatter",
    # checks
    "comparable",
    "check_duplicates",
    "check_cross_file_conflicts",
    "path_matches",
    "check_runtime_paths",
    "check_raw_rewrites",
    "check_wiki_curation",
    "check_provider_matrix",
    "check_possible_secrets",
    "check_optional_secret_scanner",
    "check_gitignore",
    "check_learning_status_conflict",
    "check_council_synthesis_stale",
    # preflight
    "parse_task_file_optional",
    "collect_preflight",
    "cmd_preflight",
    # plan
    "utc_now",
    "sha256_text",
    "sha256_file",
    "canonical_json",
    "plan_id",
    "task_refs",
    "plan_task_imports",
    "build_plan",
    "load_plan",
    "task_import_block",
    "task_import_validation",
    "live_task_conflicts",
    "stale_plan_findings",
    "acquire_import_lock",
    "release_import_lock",
    "append_imports_atomic",
    "log_imports",
    "import_result",
    "source_state",
    "plan_wiki_changes",
    "required_confirmations",
    "safe_wiki_rel",
    "proposal_gate_findings",
    "proposal_result",
    "proposal_meta",
    "write_proposal_artifacts",
    "cmd_plan",
    "cmd_import_tasks",
    "cmd_write_wiki_proposals",
]
