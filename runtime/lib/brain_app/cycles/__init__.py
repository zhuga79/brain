"""Периодические команды Brain: queue, review, sync, validate и их обвязка.

Четыре цикла родились по отдельности и к моменту сборки этого пакета совпадали
на три четверти: одинаковый пролог (utc_now/iso/brain_path/read_text/…),
собственная копия установщика systemd в каждом и собственный способ завести
corrective-задачу. Копии не просто дублировались — они расходились, и каждое
расхождение было тихой ошибкой в одном из циклов (см. `systemd` и `corrective`).

Здесь общее живёт в одном экземпляре:

* `systemd`   — установка .service/.timer;
* `corrective`— задача в очередь, когда цикл нашёл проблему;
* `runner`    — разбор общих аргументов, режимы dry-run/apply и вывод.
"""
