"""Сбор данных для дашборда, разложенный по предметам.

До разделения всё жило в одном файле на 945 строк: задачи, локи, совет, индекс,
граф, журнал, handoff, обучение, workspace'ы, токены и снимки состояния.
Общего между этими темами не было ничего, кроме файла, — и любая правка одной
из них требовала читать остальные.

`brain_dashboard.data` остаётся точкой входа: внешние вызывающие продолжают
звать те же имена оттуда.
"""
